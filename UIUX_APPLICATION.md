# UI/UX Pro Max 技能应用报告（股票分析系统）

> 生成时间：2026-08-07
> 技能来源：https://github.com/winnieeewang/ui-ux-pro-max-skill（7 个子技能已安装到 `~/.workbuddy/skills/`）
> 审计结论：✅ 安全（P2）——无凭据读取/无破坏性操作/无指令注入；脚本仅本地搜索/生成/校验，网络请求仅 Google Fonts + Pexels 素材

---

## 一、已安装技能清单

| 技能名 | 用途 | 本系统可应用点 |
|--------|------|---------------|
| `ui-ux-pro-max` | 主技能：84 风格/192 色板/98 UX 指南/25 图表类型设计智能库 | 全站 UI 决策 |
| `ui-styling` | Tailwind/shadcn 样式生成 | Streamlit CSS 变量体系 |
| `design` | 品牌/配色/图标生成 | 图表配色、icon |
| `design-system` | 设计 token 生成器 | CSS 变量 token 化 |
| `brand` | 品牌一致性 | 站内配色统一 |
| `banner-design` | Banner 尺寸/样式库 | 顶部横幅 |
| `slides` | 幻灯片设计 | 报告导出 |

---

## 二、技能诊断：对本股票系统的关键建议

### 1. K 线图（技能规则：Stock / Trading OHLC）

| 维度 | 技能建议 | 本系统现状 | 落地建议 |
|------|---------|-----------|---------|
| 颜色 | Bullish `#26A69A` / Bearish `#EF5350`（国际惯例绿涨红跌） | 红涨绿跌（`#dc2626`/`#16a34a`，中国习惯） | **保留红涨绿跌**（文化差异，技能规则允许按区域适配）；涨跌色对比度已达标 |
| 可见性 | 最多同时 500 根 K 线；提供 OHLC 数据表 | K线图无数量上限 | 大周期加载时截断到最近 500 根 |
| 色盲友好 | 用"填充 vs 空心"区分涨跌（非仅颜色） | 仅颜色区分 | 可选：K线实体填充（涨）/空心（跌）双编码 |
| 量能 | 成交量柱 40% 透明度置于下方 | 已有成交量副图 | 透明度适配深/浅色 |

### 2. Dashboard 布局（技能规则：Content Jumping）

| 问题 | 技能建议 | 落地 |
|------|---------|------|
| 内容跳动（异步加载导致布局位移） | 为异步内容预留空间（固定高度/aspect-ratio） | 实时速览条/指标卡预留最小高度，避免刷新时跳动 |
| 固定元素重叠 | 固定元素考虑安全区 | 顶部栏与滚动内容间距检查 |

### 3. 可访问性（技能规则：Accessibility CRITICAL）

- 对比度 ≥ 4.5:1：当前 `--text-dim: #6b7280` 在浅底上对比度约 4.8:1 ✅
- 图表信息不能只靠颜色传达：热力图已同时显示数值文本 ✅
- 交互目标 ≥ 44×44px：Streamlit 原生控件达标 ✅

### 4. 颜色 token 化（design-system 技能）

建议把 CSS 变量作为唯一颜色源（现状已基本如此），新增语义 token：

```css
:root {
  --chart-up: #dc2626;      /* 涨（中国惯例） */
  --chart-down: #16a34a;    /* 跌 */
  --chart-flat: #9ca3af;    /* 平 */
  --chart-volume: rgba(148,163,184,0.4);  /* 成交量 40% 透明度 */
  --contrast-danger: #b91c1c;  /* 高对比警示（深红） */
}
```

---

## 三、推荐落地的 3 个低风险改进（可选项，按需实施）

1. **K线图 500 根截断**：`_draw_kline` 里 `hist = hist.tail(500)`，防止大周期卡顿
2. **成交量柱 40% 透明度**：与技能规范对齐
3. **色盲双编码**：K线实体 = 涨（填充红）/ 跌（空心绿描边）——符合国际可访问性标准，同时保留中国配色语义

> ⚠️ 未动结论区/布局（用户要求保持现状）；以上均为可选微调。

---

## 四、如何使用该技能（以后）

在对话中描述 UI 任务时，直接说"用 ui-ux-pro-max 技能查一下 xxx 的设计规则"即可触发；
或手动查询：`python ~/.workbuddy/skills/ui-ux-pro-max/scripts/search.py "<查询>" --domain <ux|color|typography|chart|style>`
